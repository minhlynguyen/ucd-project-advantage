import { createContext } from 'react';

// create a context for solutions page, make it easier to transfer variables in components tree
const SolutionsContext = createContext();

export default SolutionsContext;
